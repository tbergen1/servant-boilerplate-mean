angular.module('appDashboard').controller('DashboardController', ['$scope', '$timeout', '$modal', function ($scope, $timeout, $modal) {
    
    $scope.initialize = function() {
        // Defaults
    };

}]);